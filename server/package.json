{
  "name": "bridge-server",
  "version": "1.0.0",
  "description": "WebSocket + TCP server bridge for MT5 EA",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.13.0"
  }
}

